import { describe, it, expect } from '@jest/globals';
import * as service from '../services/sale.service';

describe('sale.service service', () => {
  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
